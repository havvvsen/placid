services:
  placid-web:
    image: ghcr.io/${GHCR_USERNAME}/placid-web:latest
    container_name: "placid-web"
    restart: always
    ports:
      - 80:80
  placid-db:
    image: postgres:18-alpine
    container_name: "placid-postgres-database"
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgresql:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
  migrations:
    image: kukymbr/goose-docker:3.27.2
    environment:
      GOOSE_DRIVER: ${GOOSE_DRIVER}
      GOOSE_DBSTRING: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?sslmode=disable
      GOOSE_COMMAND: up
    volumes:
      - ./placid-backend/data/migrations:/migrations
    depends_on:
      placid-db:
        condition: service_healthy
  api:
    image: ghcr.io/${GHCR_USERNAME}/placid-backend:latest
    container_name: "placid-api"
    ports:
      - ${API_PORT}:${API_PORT}
    depends_on:
      placid-db:
        condition: service_healthy
    environment:
      ALLOWED_CORS_ORIGINS: ${ALLOWED_CORS_ORIGINS}
      API_HOST: ${API_HOST}
      API_PORT: ${API_PORT}
      API_VERSION: ${API_VERSION}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_PORT: ${POSTGRES_PORT}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
  file-server:
    image: ghcr.io/${GHCR_USERNAME}/placid-file-server:latest
    container_name: "placid-file-server"
    ports:
      - ${FILE_SERVER_PORT}:${FILE_SERVER_PORT}
    environment:
      FILE_SERVER_PORT: ${FILE_SERVER_PORT}
      FILE_SERVER_BASE_PATH: ${FILE_SERVER_BASE_PATH}
volumes:
  postgresql:
